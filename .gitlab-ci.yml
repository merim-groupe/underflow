include:
  - project: 'devops/ci-template'
    file: '/java-build.gitlab-ci.yml'

build:
  extends: .maven-build

deploy:
  extends: .maven-deploy
  needs:
    - build
  rules:
    - if: $CI_COMMIT_TAG
    - if: $CI_COMMIT_TAG == null
      when: manual
  retry: 2
